import React from 'react';

// functions
import { useTitle } from '../helper/functions';

const AboutUs = () => {

    useTitle('درباره ما')

    return (
        <div>
            comign soon
        </div>
    );
};

export default AboutUs;