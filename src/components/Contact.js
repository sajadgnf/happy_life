import React from 'react';

// functions
import { useTitle } from '../helper/functions';

const Contact = () => {

    useTitle("تماس با ما")

    return (
        <div>
            coming soon
        </div>
    );
};

export default Contact;